<?xml version="1.0" encoding="utf-8"?>
<component name="LoadMusicListTask" extends="Task">
    <interface>
        <field id="itemJson" type="assocarray" />
        <!-- An album's tracks, an artist's albums or a playlist's entries, as item json -->
        <field id="items" type="array" />
        <field id="done" type="boolean" alwaysNotify="true" />
    </interface>
</component>
